/// @desc Destroy Systems

// Destroy particle system
part_system_destroy(part_system);

// Destroy particle types
part_type_destroy(part_type_airburst);
part_type_destroy(part_type_debris);
part_type_destroy(part_type_windtrail);